
# Initializing PostgreSQL for "production"
spring:
    datasource:
        url: ${PROD_DB_URL}
        username: ${PROD_DB_USERNAME}
        password: ${PROD_DB_PASSWORD}
        driver-class-name: org.postgresql.Driver
    jpa:
        database: postgresql
        generate-ddl: true